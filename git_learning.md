#first
```bash
$ cd /tmp
$ mkdir git_practice
$ cd git-practice
$ git init # initialize this directory
$ git status # status query
$ git add <file> # add the file into index
$ git add *.py #add all the pyhon file into index
$ git add --all # all into index
$ git commit -m "init commit practice"
```
