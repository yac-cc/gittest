## git note
to use git

```shell
$ git commit

```

git branch

```shell
$git branch {branch1}
$git checkout {branch1}
$git checkout {file} ;git branch
$git checkout -b <your-branch-name>
```

git rebase
